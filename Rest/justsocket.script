socket_open("5783", "0.0.0.0", 30003);


while (True):
    data = socket_read_string("5783", 1024);  # Read up to 1024 characters of incoming data
    if data != "":
        # Process the received data as needed
        textmsg("Received data: " + data)

socket_close("5783");  # Close the socket connection